import React from 'react';
import { LoginForm } from '../components/LoginForm';

export const LoginPage = ({history}) => {
    return (
        <React.Fragment>
            <LoginForm/>
        </React.Fragment>
    )
}